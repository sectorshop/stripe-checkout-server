{
  "name": "stripe-checkout-server",
  "version": "1.0.0",
  "scripts": {
    "start": "echo 'This is a serverless project — no start needed'"
  },
  "dependencies": {
    "stripe": "^12.0.0"
  }
}
